@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model IEnumerable<mvc.Models.Question>

@{
    ViewData["Title"] = Localizer["TitlePage"];
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

    <h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">@Localizer["Create"]</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuestionText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.QuestionText)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.QuestionId">@Localizer["Edit"]</a> |
                    <a asp-action="Details" asp-route-id="@item.QuestionId">@Localizer["Details"]</a> |
                    <a asp-action="Delete" asp-route-id="@item.QuestionId">@Localizer["Delete"]</a>
                </td>
            </tr>
        }
    </tbody>
</table>
